import {
  StyleSheet,
  Text,
  View,
  ScrollView,
  KeyboardAvoidingView,
  TextInput,
  Pressable,
  Image,
} from "react-native";
import React, {
  useState,
  useContext,
  useLayoutEffect,
  useEffect,
  useRef,
} from "react";

const ChatMessagesScreen = () => {
  return (
    <View>
      <Text>ChatMessagesScreen</Text>
    </View>
  );
};

export default ChatMessagesScreen;

const styles = StyleSheet.create({});
